@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vue3 測試頁面</title>
</head>
<body>
    <div id="CommentApp" class="input-wrap row Comment">
        <label class="input-title col-lg-2 col-form-label">Test Input</label>
        <div class="col-lg-8">
            <input name="Comment" id="Comment" v-model="commentMsg" type="text" data-autoform-item="Edit" maxlength=50 class="form-control required pointer" style="background-color: rgb(255, 255, 255);">
        </div>
    </div>

    <div id="app">
        <p>{{ message }}</p>
        <button v-on:click="AddOne">test</button>
    </div>

    <script src="~/bundle.js"></script>
    <!--<script>
        const { createApp } = Vue

        let app = createApp({
            data() {
                return {
                    message: 5
                };
            },
            methods: {
                AddOne() {
                    let self = this;
                    console.log("what!");
                    self.message++;
                    commentApp.commentMsg = self.message;
                }
            }
        }).mount('#app');

        let commentApp = createApp({
            data() {
                return {
                    commentMsg: 3
                };
            }
        }).mount('#CommentApp')
    </script>-->
</body>
</html>


